{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Geovana Lamas",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon ./src/server.js",
    "reset": "yarn sequelize db:migrate:undo:all && yarn sequelize db:migrate && yarn sequelize db:seed:all"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mysql2": "^2.2.5",
    "sequelize": "^6.6.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.12",
    "sequelize-cli": "^6.2.0"
  }
}
